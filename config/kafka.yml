# These config values will be shared by all environments but can be overridden.
common: &common
  client_id: 'teste-backend'
  brokers:
    - 127.0.0.1:9092

development:
  <<: *common

test:
  <<: *common

staging:
  <<: *common

production:
  <<: *common
